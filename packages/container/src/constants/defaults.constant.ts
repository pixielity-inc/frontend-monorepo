/**
 * Defaults Constant
 *
 * Default configuration values used throughout the dependency injection system.
 */

/**
 * Default configuration values
 */
export const DEFAULTS = {
  LOG_LEVEL: "info" as const,
  SCOPE: "Singleton" as const,
} as const;
